import React from 'react';
import {
  View,
  Text,
  StyleSheet,
} from 'react-native';

function AddPost({navigation}){
  return(
    <View>
      <Text>Add Post here//form coming</Text>
    </View>
  )
};

export default AddPost;